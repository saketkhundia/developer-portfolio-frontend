import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saket.app',
  appName: 'Dev-Github-Portfolio-System',
  webDir: 'out'
};

export default config;
